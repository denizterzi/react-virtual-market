export type ICardModel = {
    id: number;
    title: string;
    quantity: string;
    price: number;
};

export type IProductModel = {
    id: number;
    title: string;
    quantity: string;
    price: number;
};